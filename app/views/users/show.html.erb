<div class="banner mb-4">
  <%= cl_image_tag @user.banner.key, class: "banner-img", alt: "Bannière de #{@user.name}" %>
  <%= cl_image_tag @user.avatar.key, class: "banner-avatar", alt: "Photo de profil de #{@user.name}" %>
</div>


<div class="d-flex mt-5">
  <div class="col-3 p-3 user-info mt-3">
    <%= render "user-info" %>
  </div>
  <div class="user-content col-9 ps-5 mt-3">
    <%= render "user-content" %>
  </div>
</div>
