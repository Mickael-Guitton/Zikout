<div class="message">
  <div class="<%= message.user == @user ? 'user-tag' : 'other-user-tag' %>">
      <% if message.user.avatar.attached? %>
        <%= cl_image_tag message.user.avatar, class: "avatar" %>
      <% end %>
      <%= message.user.name %>
  </div>
  <%= message.content %>
  <i><small><%= message.created_at.strftime("%a %b %e à %l:%M %p") %></small></i>
</div>
