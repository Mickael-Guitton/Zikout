<div class="modal">
  <h2>New event</h2>
  <%= render "event-form", event: @event %>
  <br>
  <div>
    <%= link_to "Cancel", new_venue_event_path(@venue), data: {
        controller: "modal",
        action: "modals#close",
        }, class: "cancel-button" %>
  </div>
</div>
